namespace MazeGame;

public enum Direction
{
    up,
    right,
    down,
    left
}

public enum Commands
{
   up,
   right,
   down,
   left,
    inventory,
    examine,
    open,
    use,
    save,
    load,
}